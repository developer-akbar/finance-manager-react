# Local development (example config)

# Use local MongoDB for fast testing
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB_NAME=finance-manager

# If you prefer testing against Atlas locally, replace the URI below and comment the local one above
# MONGODB_URI=mongodb+srv://akbarak219:<password>@bitetrack.qdailf6.mongodb.net/?retryWrites=true&w=majority&appName=bitetrack
# MONGODB_DB_NAME=finance-manager

JWT_SECRET=change-me-in-prod
JWT_EXPIRE=7d

PORT=5000
NODE_ENV=development

# Comma-separated origins
CORS_ORIGINS=http://localhost:5173

# Rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

